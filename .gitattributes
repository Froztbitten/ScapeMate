# Normalise to LF in the repo and in working trees. Without this, Windows
# checkouts produced CRLF and ESLint's linebreak-style rule failed on every
# line of functions/index.js.
* text=auto eol=lf

*.ttf binary
*.png binary
*.jpg binary
*.ico binary
